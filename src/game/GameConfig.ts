// Top-level Phaser configuration values. Anything that is needed before scenes
// initialise (background colour, scaling) lives here.

/** Matches theme.menu background for pre-scene canvas clear. */
export const GAME_CONFIG = {
  backgroundColor: '#050810',
} as const;
